CREATE OR REPLACE TRIGGER "TR_SVR_terRemoveLog_I_ID" BEFORE
  INSERT ON SVR_terRemoveLog FOR EACH ROW
Begin
    SELECT SQ_terRemoveLog_ID.NEXTVAL INTO :NEW.ID FROM DUAL;
End;


-- Create table
create table SVR_OPVEHICLELOG
(
  id             INTEGER not null,
  uniqueno       VARCHAR2(18),
  terminalno     VARCHAR2(20),
  vehicleid      VARCHAR2(20),
  simno          VARCHAR2(11),
  operator       VARCHAR2(50),
  oprecordtime   DATE,
  operatorip     VARCHAR2(50),
  optypeid       NUMBER(10),
  remark         VARCHAR2(100),
  longitude      NUMBER(20,6),
  latitude       NUMBER(20,6),
  bigarea        VARCHAR2(50),
  area           VARCHAR2(50),
  city           VARCHAR2(50),
  province       VARCHAR2(50),
  encryptionlong NUMBER(20,6),
  encryptionlat  NUMBER(20,6),
  userid         NUMBER(10)
)
tablespace ZL_MAIN
  pctfree 10
  initrans 1
  maxtrans 255
  storage
  (
    initial 64
    next 8
    minextents 1
    maxextents unlimited
  );
-- Add comments to the table 
comment on table SVR_OPVEHICLELOG
  is '终端拆除日志';
-- Add comments to the columns 
comment on column SVR_OPVEHICLELOG.uniqueno
  is '唯一编号';
comment on column SVR_OPVEHICLELOG.terminalno
  is '终端编号';
comment on column SVR_OPVEHICLELOG.vehicleid
  is '设备编号';
comment on column SVR_OPVEHICLELOG.simno
  is 'SIM卡号';
comment on column SVR_OPVEHICLELOG.operator
  is '操作员';
comment on column SVR_OPVEHICLELOG.oprecordtime
  is '操作时间';
comment on column SVR_OPVEHICLELOG.operatorip
  is '操作ip';
comment on column SVR_OPVEHICLELOG.optypeid
  is '操作类型ID';
comment on column SVR_OPVEHICLELOG.remark
  is '备注';
comment on column SVR_OPVEHICLELOG.longitude
  is '经度';
comment on column SVR_OPVEHICLELOG.latitude
  is '纬度';
comment on column SVR_OPVEHICLELOG.bigarea
  is '大区CODE';
comment on column SVR_OPVEHICLELOG.area
  is '区行政代码';
comment on column SVR_OPVEHICLELOG.city
  is '市行政代码';
comment on column SVR_OPVEHICLELOG.province
  is '省行政代码';
comment on column SVR_OPVEHICLELOG.encryptionlong
  is '加密经度';
comment on column SVR_OPVEHICLELOG.encryptionlat
  is '加密纬度';
comment on column SVR_OPVEHICLELOG.userid
  is '用户ID';
-- Create/Recreate primary, unique and foreign key constraints 
alter table SVR_OPVEHICLELOG
  add primary key (ID)
  using index 
  tablespace ZL_MAIN
  pctfree 10
  initrans 2
  maxtrans 255
  storage
  (
    initial 64K
    next 1M
    minextents 1
    maxextents unlimited
  );